事务的特性：
          a.原子性(Atomicity)  --保证每个操作要不被完成，要不就不做
          b.一致性(Consistency) --分布式数据库保持一致状态
          c.隔离性(Isolation)   --事务并发执行并不会影响彼此
          d.持久性(Durablity)   --所谓持久就是写入程序，数据的持久化操作，一旦写入系统就不会被改变

区块链：恶意节点，错误节点
      恶意节点：任意修改节点就会被认为恶意节点
